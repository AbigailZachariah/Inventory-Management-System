# Java Console Inventory Management System

## 🚀 Overview
This is a simple, console-based *Inventory Management System* built in Java. It allows a user to manage product inventory by performing basic operations (Add,view,delete) through a text-based menu interface.

---

## ✨ Features
* *Add Product:* Register a new product with an ID, Name, Quantity, and Price.
* *View Products:* Display a formatted list of all current items in the inventory.
* *Update Product:* Find an existing product by ID and modify its Name, Quantity, or Price.
* *Delete Product:* Find and permanently remove a product from the inventory by ID.

---

## 🛠 Technology Used
* *Language:* Java
* *Core Data Structure:* ArrayList (Used to store synchronized lists of product IDs, names, prices, and quantities.)


---

## 📁 File Structure
The project consists of a single Java file:

* InventorySystem.java: Contains the main class, main method, and all inventory management methods (addProduct, viewProduct, updateProduct, deleteProduct).
